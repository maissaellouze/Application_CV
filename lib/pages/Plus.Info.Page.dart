// plus_info_page.dart
import 'package:flutter/material.dart';

class PlusInfoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('PlusInfoPage'),
      ),
      body: Center(
        child: Text('This is the PlusInfoPage.'),
      ),
    );
  }
}
